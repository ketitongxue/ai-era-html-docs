# CLAUDE.md

本仓库是 GitHub Pages 文档站（推送到 main 即自动发布）。

## 工作约定

- 每次 commit 之后必须立即 `git push origin main`，不要只 commit 不 push。
- 网站文章在 `docs/` 下，仅当文件样式/内容风格与既有页面一致时才新增页面。
- 代码块统一使用 macOS 窗口样式（`#1a1b26` 代码区 + `#2d2d3a` 标题栏 + 红黄绿圆点 + Fira Code/Tokyo Night 高亮，参考各文章页 `<style>` 中的 `pre` 规则与页尾高亮脚本）。
